# SSV Modifications

## 文档目录

[玩家数据导入导出规则](docs/player-data.md)

## 版本元数据导出

> generated by Codex

作用在某个已修改过的客户端版本的 versions/版本 目录上，以导出该版本的客户端数据，以供导出导入程序使用

```powershell
uv run python tools/generate_partial_export_metadata.py
```

```powershell
uv run python tools/generate_partial_export_metadata.py \
--source "D:\\2ds-client\versions\3-3-5" \
--output "D:\\metadata_3-3-5.zip"
```

目前导出的 ZIP 内容（2026-02-24）：

- `options.txt`（原始文件副本）
- `hashes/mods.sha1`（每行一个去重后的 SHA-1）
- `hashes/resourcepacks.sha1`（每行一个去重后的 SHA-1）
- `hashes/shaderpacks.sha1`（每行一个去重后的 SHA-1）

## 版本元数据拉取格式

各个版本的元数据并没有将所对应的版本号记录在文件中，只用链接最后的文件名来标注

链接：<https://1812378450.v.123pan.cn/1812378450/2ds/client/settings/ssv_settings_3-3-5.zip>
